/**
 * 测试成员变量(实例变量)
 * 成员变量：即在方法外部，类的内部定义的变量。
 * 从属于对象，生命周期伴随对象的始终。
 * @author an
 *
 */
public class TestMemberVariable 
{
	int a; //成员变量，系统默认为0，也可以手动初始化（赋值）
	/**
	 * 成员变量如果不初始化，会被自动初始化，默认值如下：
	 * int     默认值      0
	 * double  默认值      0.0
	 * char    默认值      ‘\u0000’
	 * boolean 默认值      false
	 * @author an
	 */
	
	public static void mian(String[] args)
	{
		
	}
}